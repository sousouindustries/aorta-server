# Automatically added by dh_installinit
if [ -x "/etc/init.d/aorta-server" ] || [ -e "/etc/init/aorta-server.conf" ]; then
	invoke-rc.d aorta-server stop || exit $?
fi
# End automatically added section
